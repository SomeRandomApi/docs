import { Code, Callout } from "nextra/components";
import colors from "../../../components/colors";
import Image from 'next/image';
import greenTickIcon from '../../../public/images/icons/green-checkmark.svg'
import redCrossIcon from "../../../public/images/icons/red-cross.svg";

## Chatbot endpoint
<Callout type="info">
    <strong>Note:</strong> This endpoint requires an API key to use. If you do not have one, you can read how to generate it on the [SRA Dashboard](https://some-random-api.com/user/settings). Premium users also get increased [rate limits](/ratelimits/tokens) for this endpoint.
</Callout>

### <Code style={{ fontWeight: 400 }}><span style={{color: colors.requests["GET"]}}>GET</span> /chatbot</Code>

**Query Requirements:**<br/>

{
<table className="queryParamTable">
  <thead>
    <tr>
      <th>Query Parameter</th>
      <th>Required?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><Code>message</Code></td>
      <td>
        <Image loading="lazy" style={{ margin: 'auto' }} src={greenTickIcon} alt="green tick" width={16} height={16} quality="100" />
      </td>
    </tr>
    <tr>
      <td><Code>memoryKey (premium only)</Code></td>
      <td>
        <Image
          loading="lazy"
          style={{ margin: "auto" }}
          src={redCrossIcon}
          alt="red cross"
          width={16}
          height={16}
          quality="100"
        />
      </td>
    </tr>
  </tbody>
</table>
}

**Example Response:**
```url filename="URL"
https://api.some-random-api.com/chatbot?message=...&memoryKey=uniqueId
```

```json filename="Response"
{
  "response": "Hey there! *smiling* It's great to see you! How are you doing today?"
}
```