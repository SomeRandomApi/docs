import { Code, Callout, Tabs } from "nextra/components";
import Image from "next/image";
import colors from "../../../components/colors";
import greenTickIcon from "../../../public/images/icons/green-checkmark.svg";

## Pet Pet endpoint
<Callout type="info">
    <strong>Note:</strong> This endpoint uses the `/premium/` prefix. However, it does not require an API key to use. It is available to all users.
</Callout>

### <Code style={{ fontWeight: 400 }}><span style={{color: colors.requests["GET"]}}>GET</span> /premium/petpet</Code>

**Query Requirements:**<br/>
{

<table className="queryParamTable">
  <thead>
    <tr>
      <th>Query Parameter</th>
      <th>Required?</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <Code>avatar</Code>
      </td>
      <td>
        <Image
          loading="lazy"
          style={{ margin: "auto" }}
          src={greenTickIcon}
          alt="green tick"
          width={16}
          height={16}
          quality="100"
        />
      </td>
      <td>Must be a valid url and PNG format</td>
    </tr>
  </tbody>
</table>
}

**Example image:**

<Image
  loading="lazy"
  src="/images/canvas/petpet.gif"
  alt="pet pet"
  width={256}
  height={256}
  quality="100"
/>
