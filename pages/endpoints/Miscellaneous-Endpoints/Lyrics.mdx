import { Code, Callout } from "nextra/components";
import colors from "../../../components/colors";
import Image from 'next/image'
import greenTickIcon from '../../../public/images/icons/green-checkmark.svg'

## Lyrics endpoint

### <Code style={{ fontWeight: 400 }}><span style={{color: colors.requests["GET"]}}>GET</span> /lyrics</Code>

<Callout type="warning">Disclaimer: What the API returns is not guaranteed to return the correct lyrics or have a matching title and author.</Callout>

**Query Requirements:**<br/>
{
<table className="queryParamTable">
  <thead>
    <tr>
      <th>Query Parameter</th>
      <th>Required?</th>
    </tr>
  </thead>
  <tbody>
      <tr>
      <td><Code>title</Code></td>
      <td>
        <Image loading="lazy" style={{ margin: 'auto' }} src={greenTickIcon} alt="green tick" width={16} height={16} quality="100" />
      </td>
    </tr>
  </tbody>
</table>
}

**Example Response:**
```url filename="URL"
https://api.some-random-api.com/lyrics?title=...
```

```json filename="Response"
{
  "title": "...",
  "artist": "...",
  "lyrics": "...",
  "thumbnail": "https://...",
  "url": "https://..."
}
```
